# Changelog

## 0.3.0

First public release.

- Open a file from a Dev Container with the host's default application
- Reveal a file in the host's file manager, with the file selected
- Translate container paths to host paths by decoding the Dev Container authority
- `openOnHost.pathMap` for cases the automatic translation cannot cover
- English and Japanese UI, following the VS Code display language
